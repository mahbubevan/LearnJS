// Statements can be written in the same line separetd by semicolon ;
let a = 5; let b =5; let c = a+b; 
console.log(c);

// JS also ignores multiple spaces...

// JavaScript Line Length and Line Breaks

document.getElementById("print").innerHTML = 
"Hello Evan"

// JavaScript Code Blocks 
// JS statements can be grouped together in code blocks {}

function changeHtml(){
    document.getElementById("print").innerHTML = 
    "Changed Inside Function"
}

// changeHtml()

/*
    var => Declare a variable
    let => Declare a variable for specific block
    const => Declare a block constant
    if => Marks block of statement to be executed on condition
    for => Marks block of statement to be executed on loop 
    switch => Marks block of statement to be executed for different cases
    function => Declare a function 
    try => Implements error handling 
    
*/